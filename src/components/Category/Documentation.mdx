import { Category } from "./Category";
import { View, ScrollView } from "react-native";
import * as Stories from "./Category.stories";
import { RenderAllStories } from "../RenderAllStories";

<ScrollView style={{ flex: 1 }} contentContainerStyle={{ paddingBottom: 300, width: "100%" }} showsVerticalScrollIndicator={false} >

# Category

<View style={{ alignItems: "flex-start" }}>
  <RenderAllStories StoryExports={Stories} Component={Category} />
</View>

# Usage

```tsx
<Category title="Pizza" photoUrl="someurl" />
```

</ScrollView>
